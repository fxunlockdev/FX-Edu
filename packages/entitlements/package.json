{
  "name": "@fxunlock/entitlements",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, I/O-free entitlement decision logic for FX Academy (plan→feature matrix, PRD §5).",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "echo \"(entitlements) no lint configured\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
